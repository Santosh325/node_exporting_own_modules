const add = (a,b) => {
    return a + b;
}

const multiply = (a, b) => {
    return a * b;
}

const subtraction = (a, b) => {
    return a - b;
}

const name = "santosh thapa"
module.exports = {
    add,
    multiply,
    subtraction,
    name
}